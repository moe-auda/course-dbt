version: 2

models:
      - name: stg_postgres__events
        descripton: "a model with user events"
        columns:
          - name: event_id
            tests:
              - not_null
              - unique
          - name: user_id
            tests:
              - relationships:
                  to: ref('stg_postgres__users')
                  field: user_id
          - name: event_type
            tests:
              - accepted_values:
                  values:
                    - checkout
                    - package_shipped
                    - add_to_cart
                    - page_view
      - name: stg_postgres__orders
        descripton: "a model with users orders"
        columns:
          - name: order_id
            tests:
              - unique
              - not_null
      - name: stg_postgres__order_items
        descripton: "a model with order info"
        columns:
          - name: order_id
            tests:
              - not_null
          - name: product_id
            tests:
              - not_null          
      - name: stg_postgres__addresses
        descripton: "a model with users addresses"     
        columns:
          - name: address_id
            tests:
              - not_null
              - unique
          - name: zipcode
            tests:
              - not_null
      - name: stg_postgres__products
        descripton: "a mode with product info"
        columns:
          - name: product_id
            tests:
              - unique
              - not_null         
      - name: stg_postgres__promos
        descripton: "a model with promo info"
        columns:
          - name: promo_id
            tests:
              - unique
              - not_null    
      - name: stg_postgres__users
        descripton: "a model with user info"
        columns:
          - name: user_id
            tests:
              - unique
              - not_null   